
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:9

    steps:
      - checkout
      - run:
          command: |
            ls
      - restore_cache:
          key: v1-node-modules-{{ checksum "/react/countdown/yarn.lock" }}
      - run:
          name: Install dependencies
          command: |
            cd react/countdown
            yarn install
      - save_cache:
          paths:
            - ~/project/node_modules
          key: v1-node-modules-{{ checksum "yarn.lock" }}

      # run tests!
      # - run: yarn test